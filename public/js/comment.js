const commentSub = async (event) => {
    event.preventDefault();

    const content = document.querySelector('#comment').value.trim();
    const post_id = document.querySelector()
    // give button a class and getattribute to target id of user id
}